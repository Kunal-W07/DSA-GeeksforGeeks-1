unordered_map<int,int> hash;
       
       for(int i=0; i<n; i++){
           hash[arr[i]]++;
       }
       
       //finding
       for(int i=0; i<n; i++){
           
           if(hash[arr[i]] < 2){
               return arr[i];
           }
           
       }
