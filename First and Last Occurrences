class Solution {
  public:
    vector<int> find(vector<int>& arr, int x) {
        vector<int> ans(2,-1);
        
        int n = arr.size();
        
        int i=0; 
        int j = n-1;
       
        // first occurrences
        while(i<=j){
            int mid = i +(j-i) / 2;
            
            if(arr[mid]==x){
                
                ans[0] = mid;
                
                j = mid - 1;
            }
            
            else if(arr[mid] < x){
                i = mid + 1;
            }
            else{
                j = mid - 1;
            }
        }
        
        // last occurance
       i=0;
       j = n-1;
        while(i<=j){
            int mid = i +(j-i) / 2;
            
            if(arr[mid]==x){
                
                ans[1] = mid;
                
                i = mid + 1;
            }
            
            else if(arr[mid] < x){
                i = mid + 1;
            }
            else{
                j = mid - 1;
            }
        }
       
     
       
        return ans;
    }
};
